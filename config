# =====================================================================
#  i3 config - Kali minimal VM
#  Paleta: vanta black + cyan (#00f0ff)
# =====================================================================

set $mod Mod4
set $term alacritty

# --- Paleta -----------------------------------------------------------
set $bg        #000000
set $fg        #c8d3d5
set $accent    #00f0ff
set $inactive  #1a1a1a
set $dim       #5a6567
set $urgent    #ff3b5c

# --- Fonte ------------------------------------------------------------
font pango:JetBrainsMono Nerd Font 10

# --- Aparencia das janelas -------------------------------------------
default_border pixel 2
default_floating_border pixel 2
hide_edge_borders smart
gaps inner 6
gaps outer 0
smart_gaps on

#                       border    background  text   indicator  child_border
client.focused          $accent   $bg         $fg    $accent    $accent
client.focused_inactive $inactive $bg         $dim   $inactive  $inactive
client.unfocused        $inactive $bg         $dim   $inactive  $inactive
client.urgent           $urgent   $bg         $fg    $urgent    $urgent
client.background       $bg

# --- Autostart --------------------------------------------------------
exec --no-startup-id $HOME/.config/scripts/wallpaper.sh
exec --no-startup-id /usr/bin/spice-vdagent
exec --no-startup-id dunst
exec --no-startup-id nm-applet

# =====================================================================
#  Keybinds
# =====================================================================

# --- Essenciais -------------------------------------------------------
bindsym $mod+Return exec $term
bindsym $mod+d exec --no-startup-id dmenu_run -fn 'JetBrainsMono Nerd Font-10' -nb '#000000' -nf '#c8d3d5' -sb '#00f0ff' -sf '#000000' -p 'run:'
bindsym $mod+q kill
bindsym $mod+Shift+q kill

# --- Foco (vim) -------------------------------------------------------
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+Left  focus left
bindsym $mod+Down  focus down
bindsym $mod+Up    focus up
bindsym $mod+Right focus right

# --- Mover janelas ----------------------------------------------------
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

bindsym $mod+Shift+Left  move left
bindsym $mod+Shift+Down  move down
bindsym $mod+Shift+Up    move up
bindsym $mod+Shift+Right move right

# --- Layout -----------------------------------------------------------
bindsym $mod+b split h
bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
bindsym $mod+a focus parent

# --- Workspaces -------------------------------------------------------
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

bindsym $mod+Tab workspace back_and_forth

# --- Screenshot -------------------------------------------------------
# Regiao selecionada -> area de transferencia + arquivo
bindsym $mod+Shift+s exec --no-startup-id $HOME/.config/scripts/screenshot.sh region
# Tela inteira
bindsym Print exec --no-startup-id $HOME/.config/scripts/screenshot.sh full

# --- Sessao -----------------------------------------------------------
bindsym $mod+Shift+x exec --no-startup-id i3lock -c 000000
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec --no-startup-id "i3-nagbar -t warning -m 'Sair do i3?' -B 'Sim' 'i3-msg exit'"

# --- Modo resize ------------------------------------------------------
mode "resize" {
    bindsym h resize shrink width 5 px or 5 ppt
    bindsym j resize grow height 5 px or 5 ppt
    bindsym k resize shrink height 5 px or 5 ppt
    bindsym l resize grow width 5 px or 5 ppt

    bindsym Left  resize shrink width 5 px or 5 ppt
    bindsym Down  resize grow height 5 px or 5 ppt
    bindsym Up    resize shrink height 5 px or 5 ppt
    bindsym Right resize grow width 5 px or 5 ppt

    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# --- Mouse ------------------------------------------------------------
floating_modifier $mod
tiling_drag modifier titlebar

# =====================================================================
#  Barra
# =====================================================================
bar {
    status_command i3status
    position top
    height 26
    font pango:JetBrainsMono Nerd Font 10
    separator_symbol " · "
    strip_workspace_numbers no
    tray_output primary

    colors {
        background $bg
        statusline $fg
        separator  $inactive

        #                  border     background  text
        focused_workspace  $accent    $accent     $bg
        active_workspace   $inactive  $inactive   $fg
        inactive_workspace $bg        $bg         $dim
        urgent_workspace   $urgent    $urgent     $bg
        binding_mode       $urgent    $urgent     $bg
    }
}
